# GENERATED by gen-fleet.js — edit that, not this.
services:
  site-1:
    image: site-monitor-testsite:local
    build: ./test-site
    environment:
      QUEUE_SIZE: ${QUEUE_SIZE:-1000}
    restart: unless-stopped

  mon-1:
    image: site-monitor-monitor:local
    build:
      context: .
      dockerfile: docker/Dockerfile.monitor
    ports:
      - "${FLEET_BIND:-127.0.0.1}:6080:6080"
    environment:
      NTFY_SERVER: ${NTFY_SERVER:-https://ntfy.sh}
      NTFY_TOPIC: ${NTFY_TOPIC:-}
      NTFY_TOKEN: ${NTFY_TOKEN:-}
      VNC_PASSWORD: ${VNC_PASSWORD:?set VNC_PASSWORD in .env}
      LIVE_VIEW_URL: http://${MINI_HOST:-localhost}:6080/vnc.html?autoconnect=1&resize=scale&quality=3
      STATUS_PORT: "7100"
    volumes:
      - ./monitor/configs/inst-1.json:/app/config.json:ro
      - mon-1-data:/app/data
    shm_size: 1gb
    depends_on:
      - site-1
    restart: unless-stopped

  site-2:
    image: site-monitor-testsite:local
    environment:
      QUEUE_SIZE: ${QUEUE_SIZE:-1000}
    restart: unless-stopped

  mon-2:
    image: site-monitor-monitor:local
    ports:
      - "${FLEET_BIND:-127.0.0.1}:6081:6080"
    environment:
      NTFY_SERVER: ${NTFY_SERVER:-https://ntfy.sh}
      NTFY_TOPIC: ${NTFY_TOPIC:-}
      NTFY_TOKEN: ${NTFY_TOKEN:-}
      VNC_PASSWORD: ${VNC_PASSWORD:?set VNC_PASSWORD in .env}
      LIVE_VIEW_URL: http://${MINI_HOST:-localhost}:6081/vnc.html?autoconnect=1&resize=scale&quality=3
      STATUS_PORT: "7100"
    volumes:
      - ./monitor/configs/inst-2.json:/app/config.json:ro
      - mon-2-data:/app/data
    shm_size: 1gb
    depends_on:
      - site-2
    restart: unless-stopped

  site-3:
    image: site-monitor-testsite:local
    environment:
      QUEUE_SIZE: ${QUEUE_SIZE:-1000}
    restart: unless-stopped

  mon-3:
    image: site-monitor-monitor:local
    ports:
      - "${FLEET_BIND:-127.0.0.1}:6082:6080"
    environment:
      NTFY_SERVER: ${NTFY_SERVER:-https://ntfy.sh}
      NTFY_TOPIC: ${NTFY_TOPIC:-}
      NTFY_TOKEN: ${NTFY_TOKEN:-}
      VNC_PASSWORD: ${VNC_PASSWORD:?set VNC_PASSWORD in .env}
      LIVE_VIEW_URL: http://${MINI_HOST:-localhost}:6082/vnc.html?autoconnect=1&resize=scale&quality=3
      STATUS_PORT: "7100"
    volumes:
      - ./monitor/configs/inst-3.json:/app/config.json:ro
      - mon-3-data:/app/data
    shm_size: 1gb
    depends_on:
      - site-3
    restart: unless-stopped

  site-4:
    image: site-monitor-testsite:local
    environment:
      QUEUE_SIZE: ${QUEUE_SIZE:-1000}
    restart: unless-stopped

  mon-4:
    image: site-monitor-monitor:local
    ports:
      - "${FLEET_BIND:-127.0.0.1}:6083:6080"
    environment:
      NTFY_SERVER: ${NTFY_SERVER:-https://ntfy.sh}
      NTFY_TOPIC: ${NTFY_TOPIC:-}
      NTFY_TOKEN: ${NTFY_TOKEN:-}
      VNC_PASSWORD: ${VNC_PASSWORD:?set VNC_PASSWORD in .env}
      LIVE_VIEW_URL: http://${MINI_HOST:-localhost}:6083/vnc.html?autoconnect=1&resize=scale&quality=3
      STATUS_PORT: "7100"
    volumes:
      - ./monitor/configs/inst-4.json:/app/config.json:ro
      - mon-4-data:/app/data
    shm_size: 1gb
    depends_on:
      - site-4
    restart: unless-stopped

  site-5:
    image: site-monitor-testsite:local
    environment:
      QUEUE_SIZE: ${QUEUE_SIZE:-1000}
    restart: unless-stopped

  mon-5:
    image: site-monitor-monitor:local
    ports:
      - "${FLEET_BIND:-127.0.0.1}:6084:6080"
    environment:
      NTFY_SERVER: ${NTFY_SERVER:-https://ntfy.sh}
      NTFY_TOPIC: ${NTFY_TOPIC:-}
      NTFY_TOKEN: ${NTFY_TOKEN:-}
      VNC_PASSWORD: ${VNC_PASSWORD:?set VNC_PASSWORD in .env}
      LIVE_VIEW_URL: http://${MINI_HOST:-localhost}:6084/vnc.html?autoconnect=1&resize=scale&quality=3
      STATUS_PORT: "7100"
    volumes:
      - ./monitor/configs/inst-5.json:/app/config.json:ro
      - mon-5-data:/app/data
    shm_size: 1gb
    depends_on:
      - site-5
    restart: unless-stopped

  site-6:
    image: site-monitor-testsite:local
    environment:
      QUEUE_SIZE: ${QUEUE_SIZE:-1000}
    restart: unless-stopped

  mon-6:
    image: site-monitor-monitor:local
    ports:
      - "${FLEET_BIND:-127.0.0.1}:6085:6080"
    environment:
      NTFY_SERVER: ${NTFY_SERVER:-https://ntfy.sh}
      NTFY_TOPIC: ${NTFY_TOPIC:-}
      NTFY_TOKEN: ${NTFY_TOKEN:-}
      VNC_PASSWORD: ${VNC_PASSWORD:?set VNC_PASSWORD in .env}
      LIVE_VIEW_URL: http://${MINI_HOST:-localhost}:6085/vnc.html?autoconnect=1&resize=scale&quality=3
      STATUS_PORT: "7100"
    volumes:
      - ./monitor/configs/inst-6.json:/app/config.json:ro
      - mon-6-data:/app/data
    shm_size: 1gb
    depends_on:
      - site-6
    restart: unless-stopped

  site-7:
    image: site-monitor-testsite:local
    environment:
      QUEUE_SIZE: ${QUEUE_SIZE:-1000}
    restart: unless-stopped

  mon-7:
    image: site-monitor-monitor:local
    ports:
      - "${FLEET_BIND:-127.0.0.1}:6086:6080"
    environment:
      NTFY_SERVER: ${NTFY_SERVER:-https://ntfy.sh}
      NTFY_TOPIC: ${NTFY_TOPIC:-}
      NTFY_TOKEN: ${NTFY_TOKEN:-}
      VNC_PASSWORD: ${VNC_PASSWORD:?set VNC_PASSWORD in .env}
      LIVE_VIEW_URL: http://${MINI_HOST:-localhost}:6086/vnc.html?autoconnect=1&resize=scale&quality=3
      STATUS_PORT: "7100"
    volumes:
      - ./monitor/configs/inst-7.json:/app/config.json:ro
      - mon-7-data:/app/data
    shm_size: 1gb
    depends_on:
      - site-7
    restart: unless-stopped

  site-8:
    image: site-monitor-testsite:local
    environment:
      QUEUE_SIZE: ${QUEUE_SIZE:-1000}
    restart: unless-stopped

  mon-8:
    image: site-monitor-monitor:local
    ports:
      - "${FLEET_BIND:-127.0.0.1}:6087:6080"
    environment:
      NTFY_SERVER: ${NTFY_SERVER:-https://ntfy.sh}
      NTFY_TOPIC: ${NTFY_TOPIC:-}
      NTFY_TOKEN: ${NTFY_TOKEN:-}
      VNC_PASSWORD: ${VNC_PASSWORD:?set VNC_PASSWORD in .env}
      LIVE_VIEW_URL: http://${MINI_HOST:-localhost}:6087/vnc.html?autoconnect=1&resize=scale&quality=3
      STATUS_PORT: "7100"
    volumes:
      - ./monitor/configs/inst-8.json:/app/config.json:ro
      - mon-8-data:/app/data
    shm_size: 1gb
    depends_on:
      - site-8
    restart: unless-stopped

  site-9:
    image: site-monitor-testsite:local
    environment:
      QUEUE_SIZE: ${QUEUE_SIZE:-1000}
    restart: unless-stopped

  mon-9:
    image: site-monitor-monitor:local
    ports:
      - "${FLEET_BIND:-127.0.0.1}:6088:6080"
    environment:
      NTFY_SERVER: ${NTFY_SERVER:-https://ntfy.sh}
      NTFY_TOPIC: ${NTFY_TOPIC:-}
      NTFY_TOKEN: ${NTFY_TOKEN:-}
      VNC_PASSWORD: ${VNC_PASSWORD:?set VNC_PASSWORD in .env}
      LIVE_VIEW_URL: http://${MINI_HOST:-localhost}:6088/vnc.html?autoconnect=1&resize=scale&quality=3
      STATUS_PORT: "7100"
    volumes:
      - ./monitor/configs/inst-9.json:/app/config.json:ro
      - mon-9-data:/app/data
    shm_size: 1gb
    depends_on:
      - site-9
    restart: unless-stopped

  site-10:
    image: site-monitor-testsite:local
    environment:
      QUEUE_SIZE: ${QUEUE_SIZE:-1000}
    restart: unless-stopped

  mon-10:
    image: site-monitor-monitor:local
    ports:
      - "${FLEET_BIND:-127.0.0.1}:6089:6080"
    environment:
      NTFY_SERVER: ${NTFY_SERVER:-https://ntfy.sh}
      NTFY_TOPIC: ${NTFY_TOPIC:-}
      NTFY_TOKEN: ${NTFY_TOKEN:-}
      VNC_PASSWORD: ${VNC_PASSWORD:?set VNC_PASSWORD in .env}
      LIVE_VIEW_URL: http://${MINI_HOST:-localhost}:6089/vnc.html?autoconnect=1&resize=scale&quality=3
      STATUS_PORT: "7100"
    volumes:
      - ./monitor/configs/inst-10.json:/app/config.json:ro
      - mon-10-data:/app/data
    shm_size: 1gb
    depends_on:
      - site-10
    restart: unless-stopped

  dashboard:
    image: site-monitor-dashboard:local
    build: ./dashboard
    ports:
      # 7300 outside: macOS AirPlay Receiver squats on 7000 (host side).
      - "${FLEET_BIND:-127.0.0.1}:${DASHBOARD_PORT:-7300}:7000"
    environment:
      FLEET: '[{"label":"inst-1","watcher":"http://mon-1:7100","site":"http://site-1:8080","novnc":"http://${MINI_HOST:-localhost}:6080/vnc.html"},{"label":"inst-2","watcher":"http://mon-2:7100","site":"http://site-2:8080","novnc":"http://${MINI_HOST:-localhost}:6081/vnc.html"},{"label":"inst-3","watcher":"http://mon-3:7100","site":"http://site-3:8080","novnc":"http://${MINI_HOST:-localhost}:6082/vnc.html"},{"label":"inst-4","watcher":"http://mon-4:7100","site":"http://site-4:8080","novnc":"http://${MINI_HOST:-localhost}:6083/vnc.html"},{"label":"inst-5","watcher":"http://mon-5:7100","site":"http://site-5:8080","novnc":"http://${MINI_HOST:-localhost}:6084/vnc.html"},{"label":"inst-6","watcher":"http://mon-6:7100","site":"http://site-6:8080","novnc":"http://${MINI_HOST:-localhost}:6085/vnc.html"},{"label":"inst-7","watcher":"http://mon-7:7100","site":"http://site-7:8080","novnc":"http://${MINI_HOST:-localhost}:6086/vnc.html"},{"label":"inst-8","watcher":"http://mon-8:7100","site":"http://site-8:8080","novnc":"http://${MINI_HOST:-localhost}:6087/vnc.html"},{"label":"inst-9","watcher":"http://mon-9:7100","site":"http://site-9:8080","novnc":"http://${MINI_HOST:-localhost}:6088/vnc.html"},{"label":"inst-10","watcher":"http://mon-10:7100","site":"http://site-10:8080","novnc":"http://${MINI_HOST:-localhost}:6089/vnc.html"}]'
    depends_on:
      - mon-1
      - mon-2
      - mon-3
      - mon-4
      - mon-5
      - mon-6
      - mon-7
      - mon-8
      - mon-9
      - mon-10
    restart: unless-stopped

volumes:
  mon-1-data:
  mon-2-data:
  mon-3-data:
  mon-4-data:
  mon-5-data:
  mon-6-data:
  mon-7-data:
  mon-8-data:
  mon-9-data:
  mon-10-data:
